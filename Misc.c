#include	<ctype.h>#include	<stdlib.h>#include	<string.h>void	itoa(int n, char s[]);void	ltoa(long n, char s[]);void	reverse(char s[]);Boolean	AlphaLess(char s1[], char s2[]);Boolean IsLess(char s1[], char s2[], Boolean option);Boolean IsNumber(char s[]);Ptr		StringEnd(Ptr beginning);void	itoa(int n, char s[]){	register int	i, sign;		if ((sign = n) < 0)		n = -n;	i = 0;	do {		s[i++] = n % 10 + '0';	} while ((n /= 10) > 0);	if (sign < 0)		s[i++] = '-';	s[i] = '\0';	reverse(s);}void	reverse(char s[]){	register int	c, i, j;		for (i=0, j = strlen(s)-1; i<j; i++, j--) {		c = s[i];		s[i] = s[j];		s[j] = c;	}}void	ltoa(long n, char s[]){	register int	i, sign;		if ((sign = n) < 0)		n = -n;	i = 0;	do {		s[i++] = n % 10 + '0';	} while ((n /= 10) > 0);	if (sign < 0)		s[i++] = '-';	s[i] = '\0';	reverse(s);}Boolean	AlphaLess(char s1[], char s2[]){	int				length1,					length2;	register int	counter;				length1 = strlen(s1);	length2 = strlen(s2);	for (counter=0; (counter<length1 && counter<length2); counter++) {		if (s1[counter] < s2[counter])			return TRUE;		else			if (s1[counter] > s2[counter])				return FALSE;	}		if (length1 < length2)		return TRUE;	else		return FALSE;	}Boolean IsLess(char s1[], char s2[], Boolean option){	long			number1,					number2;	int				length1,					length2;	register int	counter;		switch (option) {		case TRUE:			length1 = strlen(s1);			length2 = strlen(s2);			for (counter=0; (counter<length1 && counter<length2); counter++) {				if (s1[counter] > s2[counter])					return FALSE;			}			if (length1 < length2)				return TRUE;			else				return FALSE;			break;		case FALSE:			number1 = atol(s1);			number2 = atol(s2);			if (number1 < number2)				return TRUE;			else				return FALSE;			break;	}}Boolean IsNumber(char s[]){	register int	counter;		for (counter=0; counter<strlen(s); counter++)		if (!isdigit(s[counter]))			return FALSE;	return TRUE;}Ptr		StringEnd(Ptr beginning){	while (TRUE) {		if ((*beginning == '\t') || (*beginning == '\r'))			return beginning;		beginning++;	}}	